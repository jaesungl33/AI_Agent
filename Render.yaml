services:
  - type: web
    name: ai-agent-backend
    env: python
    plan: free
    buildCommand: pip install -r backend_api/requirements.txt
    startCommand: cd backend_api && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.9"
      # Add your LLM provider keys if needed, e.g.:
      # - key: QWEN_API_KEY
      #   sync: false
      # - key: QWEN_BASE_URL
      #   sync: false

